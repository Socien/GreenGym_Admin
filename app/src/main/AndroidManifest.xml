<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.greengym_admin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/admin"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/admin_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Greengym_Admin">
        <activity android:name=".Equip_detail"></activity>
        <activity android:name=".equip_item" />
        <activity android:name=".dialog" />
        <activity android:name=".item" />
        <activity android:name=".ReportActivity" />
        <activity android:name=".EquipActivity" />
        <activity android:name=".IntroActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>